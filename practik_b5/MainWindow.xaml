<Window x:Class="practik_b5.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:practik_b5"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="19"></Setter>
            <Setter Property="VerticalContentAlignment" Value="Center"></Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="FontWeight" Value="Bold"></Setter>
        </Style>
        <Style TargetType="Label">
            <Setter Property="FontSize" Value="20"></Setter>
            <Setter Property="FontWeight" Value="Bold"></Setter>
        </Style>
        <LinearGradientBrush x:Key="gradient">
            <GradientStop Color="Yellow" Offset="0"/>
            <GradientStop Color="LimeGreen" Offset="0.3"/>
            <GradientStop Color="Green" Offset="1"/>
        </LinearGradientBrush>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Label Margin="4 10 0 7" Grid.Row="0">From :</Label>
        <Label Margin="30 10 10 10" Grid.Row="1">To :</Label>
        <TextBox x:Name="sourceTb" Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2" IsReadOnly="True" Text="{Binding Source}"></TextBox>
        <TextBox x:Name="destTb" Grid.Row="1" Grid.Column="1" Grid.ColumnSpan="2" IsReadOnly="True" Text="{Binding Destination}"></TextBox>
        <Button Click="SourceBtn" Background="{StaticResource gradient}" Height="40" Width="120" Margin="10" Grid.Row="0" Grid.Column="4" FontSize="25">Browse</Button>
        <Button Click="DestBtn" Background="{StaticResource gradient}" Height="40" Width="120" Margin="10" Grid.Row="1" Grid.Column="4" FontSize="25">Browse</Button>
        <Button Click="CopyBtn" Background="{StaticResource gradient}" Height="70" Width="760" Padding="0 0 0 7" Margin="10" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="4" FontSize="40">Copy</Button>
        <ListBox ItemsSource="{Binding Processes}" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Grid.Row="4" Grid.ColumnSpan="4" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <ListBoxItem>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="250"></ColumnDefinition>
                                <ColumnDefinition Width="50"></ColumnDefinition>
                                <ColumnDefinition></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Filename}" Margin="5" TextTrimming="WordEllipsis"></TextBlock>
                            <TextBlock Grid.Column="1" Margin="5"><Run Text="{Binding Percentage}"></Run> % </TextBlock>
                            <ProgressBar Grid.Column="2" Value="{Binding Percentage}"></ProgressBar>
                        </Grid>
                    </ListBoxItem>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
